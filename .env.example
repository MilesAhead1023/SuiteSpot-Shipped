# ============================================================================
# SuiteSpot Development Environment Configuration
# ============================================================================
# Copy this file to .env and configure it for your local development setup

# ============================================================================
# BUILD CONFIGURATION
# ============================================================================

# Visual Studio version and toolset
VS_VERSION=17
MSVC_TOOLSET=v143
WINDOWS_SDK_VERSION=10.0
CPP_STANDARD=20

# Build configuration (Release or Debug)
BUILD_CONFIG=Release
BUILD_PLATFORM=x64

# ============================================================================
# PATHS - Visual Studio & Build Tools
# ============================================================================

# Path to vcpkg (used for nlohmann-json dependency)
VCPKG_ROOT=C:\Users\bmile\vcpkg

# Visual Studio installation path (optional, auto-detected by default)
# VS_PATH=C:\Program Files\Microsoft Visual Studio\2022\Community

# Windows SDK installation path (optional, auto-detected by default)
# WINDOWS_SDK_PATH=C:\Program Files (x86)\Windows Kits\10

# ============================================================================
# PATHS - Project Output & Artifacts
# ============================================================================

# Project root directory
PROJECT_ROOT=.

# Output directory for compiled DLL
OUTPUT_DIR=./plugins

# Intermediate build artifacts directory
BUILD_INTERMEDIATES=./build/.intermediates

# Object files and build artifacts
OBJ_DIR=./build/obj

# ============================================================================
# PATHS - BakkesMod Installation
# ============================================================================

# BakkesMod data directory (where plugin resources are deployed)
BAKKESMOD_DATA_DIR=%APPDATA%\bakkesmod\bakkesmod\data\SuiteSpot

# BakkesMod installation root (used for SDK discovery and plugin injection)
BAKKESMOD_ROOT=%APPDATA%\bakkesmod\bakkesmod

# BakkesMod plugin patch executable
BAKKESMOD_PATCH=%APPDATA%\bakkesmod\bakkesmod\bakkesmodsdk\bakkesmod-patch.exe

# ============================================================================
# PATHS - Training Data & Cache
# ============================================================================

# Directory for training pack database (training_packs.json)
TRAINING_DATA_DIR=%APPDATA%\bakkesmod\bakkesmod\data\SuiteSpot\TrainingSuite

# Directory for usage statistics (pack_usage_stats.json)
STATS_DATA_DIR=%APPDATA%\bakkesmod\bakkesmod\data\SuiteSpot\TrainingSuite

# Workshop maps cache directory
WORKSHOP_CACHE_DIR=%APPDATA%\bakkesmod\bakkesmod\data\SuiteSpot\WorkshopCache

# ============================================================================
# API CONFIGURATION
# ============================================================================

# RLMAPS API endpoint for workshop map downloads
RLMAPS_API_URL=https://rlmaps.com/api

# RLMAPS API timeout (milliseconds)
RLMAPS_API_TIMEOUT=10000

# ============================================================================
# PLUGIN CONFIGURATION
# ============================================================================

# Plugin name as registered in BakkesMod
PLUGIN_NAME=SuiteSpot

# Plugin version (auto-incremented by version.h pre-build script)
# PLUGIN_VERSION=x.x.x (auto-managed)

# Enable debug logging to console
DEBUG_LOGGING=false

# Log file path for plugin diagnostics (optional)
LOG_FILE=%APPDATA%\bakkesmod\bakkesmod\data\SuiteSpot\SuiteSpot.log

# ============================================================================
# FEATURE FLAGS - Development & Testing
# ============================================================================

# Enable verbose debug output for AutoLoadFeature
DEBUG_AUTO_LOAD=false

# Enable verbose debug output for MapManager
DEBUG_MAP_MANAGER=false

# Enable verbose debug output for WorkshopDownloader
DEBUG_WORKSHOP_DOWNLOADER=false

# Enable verbose debug output for TrainingPackManager
DEBUG_TRAINING_PACKS=false

# Enable mock data for testing (if applicable)
USE_MOCK_DATA=false

# ============================================================================
# PERFORMANCE TUNING
# ============================================================================

# Timeout for deferred execution after match-end (milliseconds)
# Used by gameWrapper->SetTimeout() in AutoLoadFeature
MATCH_END_TIMEOUT=500

# Workshop map download retry attempts
DOWNLOAD_MAX_RETRIES=3

# Workshop map download retry delay (milliseconds)
DOWNLOAD_RETRY_DELAY=1000

# Training pack database refresh interval (seconds)
# How often to reload training_packs.json from disk
PACK_REFRESH_INTERVAL=300

# UI render throttle rate (frames per second)
UI_RENDER_FPS=60

# ============================================================================
# DEVELOPMENT TOOLS
# ============================================================================

# Enable PowerShell script execution for version updates
POWERSHELL_ENABLED=true

# PowerShell execution policy for build scripts
POWERSHELL_EXEC_POLICY=Bypass

# Enable creation of debug symbols in Release build
GENERATE_DEBUG_INFO=true

# Enable link-time code generation optimization
LTCG_ENABLED=true

# ============================================================================
# CVar DEFAULTS (Plugin Configuration Variables)
# ============================================================================

# All plugin settings use the suitespot_ prefix
# These are stored in BakkesMod's cvar system and persisted

# Enable/disable the entire plugin
SUITESPOT_ENABLED=true

# Enable auto-loading training packs after matches
SUITESPOT_AUTO_LOAD_ENABLED=true

# Delay before auto-loading (milliseconds) - allows match-end sequence to complete
SUITESPOT_DELAY_QUEUE=500

# Enable auto-loading in freeplay mode
SUITESPOT_FREEPLAY_ENABLED=true

# Enable auto-loading workshop maps
SUITESPOT_WORKSHOP_ENABLED=true

# Enable loadout switching after loading maps
SUITESPOT_AUTO_LOADOUT_ENABLED=false

# ============================================================================
# NOTES FOR DEVELOPERS
# ============================================================================

# 1. REGISTRY-BASED PATHS
#    BakkesMod SDK path is automatically read from:
#    HKEY_CURRENT_USER\Software\BakkesMod\SDKLocation
#
# 2. THREAD SAFETY
#    - Training pack lists are protected by std::mutex
#    - UI rendering uses std::atomic<bool> isRenderingSettings
#    - AsyncIO operations for map discovery and downloads
#
# 3. DATA PERSISTENCE
#    All data is automatically persisted to:
#    %APPDATA%\bakkesmod\bakkesmod\data\SuiteSpot\TrainingSuite\
#
# 4. BUILD PROCESS
#    Pre-build:  CompiledScripts/update_version.ps1 increments build number
#    Post-build: bakkesmod-patch.exe injects DLL and deploys resources
#
# 5. COMMON ISSUES
#    - Game crashes on immediate load: Use SetTimeout() for deferred execution
#    - Missing training packs: Check training_packs.json is in correct path
#    - Workshop maps not found: Verify WORKSHOP_CACHE_DIR exists
